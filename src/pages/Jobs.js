import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Jobs = (props) => {
  return (
    <View>
      <Text>JOBS</Text>
    </View>
  );
};

export {Jobs};

const styles = StyleSheet.create({});
